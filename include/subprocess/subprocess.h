#include <string>

namespace NSubprocess {

////////////////////////////////////////////////////////////////////////////////

#if defined(_WIN32) || defined(_WIN64)
using TProcessHandle = HANDLE;
#else
using TProcessHandle = __pid_t;
#endif

////////////////////////////////////////////////////////////////////////////////

TProcessHandle CreateSubprocess(std::string cmd);

bool IsProcessAlive(TProcessHandle pid);

////////////////////////////////////////////////////////////////////////////////

} // namespace NSubprocess
